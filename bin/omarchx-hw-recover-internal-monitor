#!/bin/bash

# omarchx:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/omarchx/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! omarchx-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
