#!/bin/bash

# omarchx:summary=Apply the current theme keyboard color to ASUS ROG keyboards
# omarchx:hidden=true

ASUSCTL_THEME=~/.config/omarchx/current/theme/keyboard.rgb

if omarchx-cmd-present asusctl; then
  asusctl aura effect static -c $(sed 's/^#//' $ASUSCTL_THEME)
fi
