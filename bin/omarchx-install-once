#!/bin/bash

# omarchx:summary=Install the ONCE service, enable its background service, and launch the TUI.
# omarchx:requires-sudo=true

echo "Installing ONCE..."
omarchx-pkg-add once-bin

echo "Enabling ONCE background service..."
sudo systemctl enable --now once-background.service

echo -e "\nLaunching ONCE..."
once
