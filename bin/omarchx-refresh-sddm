#!/bin/bash

# omarchx:summary=Refresh the SDDM theme from default
# omarchx:requires-sudo=true

sudo rm -rf /usr/share/sddm/themes/omarchx
sudo cp -r $OMARCHX_PATH/default/sddm/omarchx /usr/share/sddm/themes/omarchx
