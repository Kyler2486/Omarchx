#!/bin/bash

# omarchx:summary=Launch a TUI or focus an existing terminal window for it
# omarchx:args=<command> [args...]

APP_ID="org.omarchx.$(basename "$1")"
LAUNCH_COMMAND="omarchx-launch-tui $@"

exec omarchx-launch-or-focus "$APP_ID" "$LAUNCH_COMMAND"
